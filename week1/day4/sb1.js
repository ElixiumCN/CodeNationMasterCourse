// array = list
// key value pair
// let & const = declaration statements
// you only declare something once
// coding = data handling

const person = {
    // key value pair
    name: "Jordan",
    age: "31",
    // multi dimensional array
    movies: ["A New Hope", "Anything from Marvel"]
};

    // multi dimensional array
let array = [[], [], []];

let array2 = [
    ["water", 1], [["pepsi", 1.5], ["tea", 1.6]]
];

console.log(array2);
console.log(array2[0]);
console.log(array2[0][0]);
console.log(array2[0][1]);
console.log(array2[2][1]);

// person.name
// person.movies[0]


const car = {
    // key value pair
    brand: "Mercedes Benz",
    model: "Luxury S Class",
    maxspeed: "133",
    insurance: "Expensive",
    extrapackages: ["upgraded audio", "leather interior"]
};

// person.name;
console.log(person.name);

// bracket notation
// used when accessing array data and keys

console.log(person["name"])

let data = "name"
console.log(person["data"])


