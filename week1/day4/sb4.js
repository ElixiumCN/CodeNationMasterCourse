const person = {
    this: "one",
    hello: "two",
    something: "three",
    movies: ["A New Hope", "Anything from Marvel"]
};

for (let i = 1; i < 3; i++) {
    console.log(person[i])
}

// dynamic variables
// e.g. loop e.g. spawn all ten NPCs in a room in a game.

// act 3. multidimensionals arrays